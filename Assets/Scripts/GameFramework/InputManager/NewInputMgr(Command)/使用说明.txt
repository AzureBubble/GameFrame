********* 命令模式的InputManager 使用文档 *********
这是使用结合了命令模式实现的旧输入系统的按键监听管理器
默认有键盘按键和鼠标按键的三种状态模式，根据枚举来区分按下，按住，抬起三种状态
还有Unity的热键监听输入，但只有Horizontal和Vertical的GetAxis()和GetAxisRaw()
如果需要其他的功能按键监听，可自己自定义命令类

如果通过新建Command来添加命令，就不要使用直接三个参数的方法来添加命令
两者只能同时使用一种方式来进行命令的注册
避免出现预想不到的结果或者问题